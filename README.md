# MEGAcmd

only 377 MB with all tools and working thumbnail generation

    docker run --rm -ti -v ${HOME}/.megaCmd:/root/.megaCmd -v /home/upload:/upload ghcr.io/jumoog/megacmd:latest mega-put /upload/ /
